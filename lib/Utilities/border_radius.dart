import 'package:flutter/material.dart';

const kSmallBorderRadius = BorderRadius.all(Radius.circular(8));
const kMediumBorderRadius = BorderRadius.all(Radius.circular(10));
const kLargeBorderRadius = BorderRadius.all(Radius.circular(15));